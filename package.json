{
  "name": "volleyball-couple-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --project demo-volleyball-couple --only auth,database \"node --test --test-force-exit tests/firebase-rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.18.0",
    "firebase-tools": "15.28.0"
  }
}
