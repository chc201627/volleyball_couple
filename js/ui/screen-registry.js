/** Where screens register themselves at parse time, keyed by destination or overlay
 * id. Its own file because the orchestrator loads last, after every screen. */
/* exported UIScreens */
var UIScreens = {};
